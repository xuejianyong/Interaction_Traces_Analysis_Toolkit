package dl;

public class CodeBackup {

	public static void main(String[] args) {
		// TODO Auto-generated method stub

		//观察环境的变化与前台页面的变化是否一致
		/*int width = env.length;
		int height = env[0].length;
		System.out.println("the size of env is:"+width+" "+height);
		for(int k=0;k<width;k++) {
			for(int z=0;z<height;z++) {
				if(env[k][z] == 1)System.out.print("*");
				else if(k==randomPosition.getPointX() && z==randomPosition.getPointY()) {
					System.out.print("/");
				}else {
					System.out.print("-");
				}
			}
			System.out.println();
		}	
		*/

		
		//观察环境的变化与前台页面的变化是否一致
		/*int width = env.length;
		int height = env[0].length;
		System.out.println("the size of env is:"+width+" "+height);
		for(int k=0;k<width;k++) {
			for(int z=0;z<height;z++) {
				if(env[k][z] == 1)System.out.print("*");
				else if(k==randomPosition.getPointX() && z==randomPosition.getPointY()) {
					System.out.print("/");
				}else {
					System.out.print("-");
				}
			}
			System.out.println();
		}*/
		
		
		
		
		//没有进入验证composite interaction的阶段，或者已经结束了对于composite interaction的验证
		/*
		if(!isEnactCompositeInteraction) {
			
		}else {//enacting composite interaction
			//learnCompositeInteraction(enactedCompositeIntraction);
			this.setEnactedInteraction(enactedCompositeIntraction);
			enactedInteractionList.add(enactedCompositeIntraction);
		}*/
		
		

		/*
		if(intendedInteraction.equals(enactedInteraction1)) {
			//this.setMood(Mood.SATISFIED);
			//intended interaction list is finished or not.
		}*/
		
		
		/*
		if(node.getUpperNodeDirection() == 1) {
			previousEnactedCompositeInteraction = enactedInteraction2;
		}else if(node.getUpperNodeDirection() ==2){
			previousEnactedCompositeInteraction = addOrGetAndReinforceCompositeInteraction(previousEnactedCompositeInteraction, enactedInteraction2);
			interactionStack.push(previousEnactedCompositeInteraction);
		}*/
		
		
		
		
		/*for(Node no : nodeList){
		if(no.getUpperNode() == null)
			root = no;
	}*/
	
		
		
		
		
		
	}

}
